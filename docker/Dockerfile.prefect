# Prefect Data Pipeline
FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim AS builder

WORKDIR /app
ENV UV_NO_CACHE=1

RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml uv.lock ./
RUN uv sync --locked --no-dev --compile-bytecode

FROM python:3.12-slim-bookworm
WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        curl \
        tesseract-ocr \
        tesseract-ocr-pol \
        tesseract-ocr-eng \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /app/.venv /app/.venv
COPY ./src/data_pipeline ./src/data_pipeline
COPY ./src/config ./src/config
COPY ./src/__init__.py ./src/__init__.py
COPY ./graph_config.yaml ./graph_config.yaml

ENV PATH="/app/.venv/bin:$PATH" \
    PREFECT_HOME=/app/.prefect \
    HOME=/app

RUN groupadd -r prefectuser \
    && useradd -r -g prefectuser prefectuser \
    && mkdir -p /app/.prefect \
    && chown prefectuser:prefectuser /app/.prefect

USER prefectuser

EXPOSE 4200

CMD ["prefect", "server", "start", "--host", "0.0.0.0", "--port", "4200"]